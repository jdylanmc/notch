# Domain docs

This is a single-context repository.

Before exploring, read root `CONTEXT.md` and relevant records in `docs/adr/`
when present, alongside the repository's existing guidance.

If those domain documents are absent, continue without creating placeholders.
Domain-modeling workflows create them when terminology or decisions are
actually resolved.

Use the glossary's vocabulary consistently. Surface conflicts with existing
architecture decision records instead of silently overriding them.
